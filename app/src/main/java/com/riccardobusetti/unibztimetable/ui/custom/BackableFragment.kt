package com.riccardobusetti.unibztimetable.ui.custom

/**
 * Interface describing a fragment that can go back.
 */
interface BackableFragment {

    /**
     * Handles the back button press.
     */
    fun onBackPressed()
}